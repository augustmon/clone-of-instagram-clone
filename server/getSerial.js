// const { SerialPort } = require("serialport");

// const port = new SerialPort({ path: "path", baudRate: 9600 });
// const parser = port.pipe(new ReadlineParser({ delimiter: "\n" })); // Read the port data
